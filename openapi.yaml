openapi: 3.0.3
info:
  title: KRAIL-CONFIG Schema API
  version: 1.0.0
paths:
  /schemas:
    get:
      summary: List all schemas
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                type: object
                properties: {}
components:
  schemas: {}
